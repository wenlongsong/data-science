# Sales — metrics

Canonical measures. Open the metric to get its exact definition and the Source-of-Truth tables to compute it
from — never reinvent the formula.

* [gross_revenue](/sales/metrics/gross_revenue.md) - Recognized gross revenue (pre-refund), USD. Use for
  "revenue", "sales", "bookings", "top line". For post-refund revenue, see the must-apply note (use
  `net_revenue` instead — add it via maintenance if a question needs it).
