# Sales — datasets

The physical dataset(s) behind the sales domain. Open one when you need a fully-qualified name, the
connection/grain notes, or a list of what tables exist.

* [orders_db](/sales/datasets/orders_db.md) - Operational order store (schema `sales`); holds the
  order-line fact table and its daily aggregate.
